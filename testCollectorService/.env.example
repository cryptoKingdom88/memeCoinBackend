# Kafka Configuration
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=trade-info

# Test Configuration
TOKEN_COUNT=20                    # Number of tokens to simulate
TRADES_PER_TOKEN=15              # Base number of trades per token per batch
TRADE_VARIATION=10               # Random variation (±10, so 5-25 trades per token)
BATCH_INTERVAL=200ms             # Interval between batches
TEST_DURATION=5m                 # How long to run the test

# Trade Simulation Parameters
MIN_PRICE=0.001                  # Minimum token price in USD
MAX_PRICE=10.0                   # Maximum token price in USD
MIN_AMOUNT=10.0                  # Minimum trade amount in USD
MAX_AMOUNT=10000.0               # Maximum trade amount in USD
BUY_PROBABILITY=0.5              # Probability of buy vs sell (0.5 = 50% buy, 50% sell)